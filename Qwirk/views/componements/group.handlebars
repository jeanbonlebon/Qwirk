<input type="checkbox" name="toggle" id="toggle" />
<label for="toggle">
  {{#each selectedGroup}}
      {{name}}
  {{/each}}
</label>
<div class="wrapper">
  {{#ifCond myself v2}}
      <div class="nav_admin row">
          <div class="col s3">

          </div>
          <div class="col s3">
              <input class="typeahead" type="text" placeholder="Add friends to your group">
          </div>
          <div class="col s6">
              <ul class="icon_admingr">
                <li><a href="#"><i class="material-icons">search</i></a></li>
                <li><a href="#"><i class="material-icons">list</i></a></li>
                <li><a href="#del_group" data-behaviour="modal" data-title="1 title of modal" data-content="1 content goes here">
                    <i class="material-icons">delete</i></a>
                </li>
              </ul>
          </div>
      </div>
  {{/ifCond}}


  <div class="wrap">
  <!--
  <p>
   {{#ifCond myself v2}}
       {{myself}} est admin du groupe
   {{else}}
       {{myself}} n'est pas admin du groupe
   {{/ifCond}}
  </p>
  -->
    <section class="friend_infos">

        {{#each selectedGroup}}
            <p>{{name}}</p>
        {{/each}}
        {{#each selectedGroupUsers}}
            <p>{{user}}</p>
        {{/each}}
    </section>
    <section class="console">

        <div class="row send">
            <div class="col s11">
                <input type="text">
            </div>
            <div class="col s1">
                <a href="#"><i class="material-icons">replay</i></a>
            </div>
        </div>

    </section>
</div>
</div>
